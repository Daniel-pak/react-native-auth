import React from 'react';
import { View } from 'react-native';


const Spinner = () => {
  return (
    <View />
  );
}

export { Spinner }; //Because this is a reusable component, we use Spinner in braces.
